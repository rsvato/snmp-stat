Revival of old SNMP based traffic collection utility
---

It uses really old SNMP4J version (like ~20 yrs), but it's still working. 
Added docker-compose with SNMP simulator to be able to run everything.
Database schema is really part of same old system, need to be cleaned up.
